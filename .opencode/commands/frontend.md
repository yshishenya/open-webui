---
description: Frontend check (typecheck + lint)
agent: build
---

Run:

- `npm run check`
- `npm run lint:frontend`

If failures appear to be pre-existing (not caused by the current diff), report them and stop; otherwise fix them.
