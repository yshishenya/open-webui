name: Migration Check

on:
  pull_request:
    branches:
      - airis_b2c
  push:
    branches:
      - airis_b2c

permissions:
  contents: read

jobs:
  migration-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Alembic migrations (upgrade/downgrade/upgrade)
        run: |
          docker compose -f docker-compose.yaml -f docker-compose.dev.yaml run --rm airis bash -lc "alembic upgrade head && alembic downgrade base && alembic upgrade head"
